services:
  - type: web
    name: satis-attribution-backend
    runtime: python
    plan: free
    autoDeployTrigger: commit
    buildCommand: pip install .
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health/database
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:Master_mind237@db.edfcupulvqhvaxvehisb.supabase.co:5432/postgres
      - key: ADMIN_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.14.3
      - key: DATABASE_CONNECT_TIMEOUT
        value: "5"